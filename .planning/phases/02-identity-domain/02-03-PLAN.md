---
phase: 02-identity-domain
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
autonomous: true
requirements: [D-12, D-13]
files_modified:
  - consent-protocol/hushh_mcp/services/one_email_kyc_service.py
  - consent-protocol/tests/services/test_one_email_kyc_service.py

must_haves:
  truths:
    - "Identity domain fields (full_name, email, phone_number, date_of_birth, address) map to the KYC consumer's `_IDENTITY_REQUIRED_FIELDS` with zero KYC schema changes"
    - "The `attr.identity.*` scope auto-resolves and consent-exports identity data to the KYC agent once identity data exists"
    - "The identity domain does not collide with the separate internal `kyc_workflow` domain"
    - "No SSN field is required or referenced by the KYC mapping (KYC required fields have none)"
  artifacts:
    - path: "consent-protocol/tests/services/test_one_email_kyc_service.py"
      provides: "tests proving identity-domain fields satisfy _IDENTITY_REQUIRED_FIELDS and attr.identity.* resolves"
      contains: "identity"
  key_links:
    - from: "one_email_kyc_service identity candidate"
      to: "_IDENTITY_REQUIRED_FIELDS"
      via: "_effective_required_fields_for_candidates domain==identity branch"
      pattern: "_IDENTITY_REQUIRED_FIELDS"
---

<objective>
Wire the new `identity` producer domain to the existing KYC consumer and verify the
`attr.identity.*` consent-export path resolves end-to-end. The KYC service already
reads `attr.identity.*` (`_DEFAULT_KYC_SCOPE` line 56) and already special-cases
`domain == "identity"` in `_effective_required_fields_for_candidates` (line 1773) —
so this wave is largely verification plus the field-shape alignment that makes the
identity producer's payload satisfy `_IDENTITY_REQUIRED_FIELDS`.

Purpose: Deliver the payoff. Because identity stores `full_name` canonically (D-B),
the existing KYC field map (`full_name`, `date_of_birth`, `address`, `phone_number`,
`email`) works with zero KYC schema changes. Confirm and lock this with tests so a
regression cannot silently break the producer→consumer contract.

Output: verified KYC field mapping for the identity domain, an `attr.identity.*`
scope-resolution test, and a no-collision assertion against `kyc_workflow`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-identity-domain/02-CONTEXT.md
@.planning/phases/02-identity-domain/02-RESEARCH.md
@.planning/phases/02-identity-domain/02-01-PLAN.md
</context>

<constraints>
- NO SSN (D-A). `_IDENTITY_REQUIRED_FIELDS` has no SSN field — do NOT add one. The
  KYC mapping must reference only the existing required fields.
- Name shape (D-B): the identity producer stores `full_name` canonical (from plan
  02-01). KYC reads `full_name`. Do NOT change the KYC field map to first/last.
- Do not collide with the separate internal `kyc_workflow` domain — the identity
  domain key is `identity`, distinct from `kyc_workflow`.
- The restricted sensitivity tier must remain force-blocked from `default_available`
  (verify, do not weaken).
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Verify and lock identity → KYC field mapping</name>
  <files>consent-protocol/hushh_mcp/services/one_email_kyc_service.py</files>
  <read_first>
    - consent-protocol/hushh_mcp/services/one_email_kyc_service.py (_DEFAULT_KYC_SCOPE / _IDENTITY_SCOPE lines 56-57; _ALLOWED_KYC_DATA_SCOPES line 63; _IDENTITY_REQUIRED_FIELDS frozenset lines 100-114 — full_name/date_of_birth/address/phone_number/email/...; _effective_required_fields_for_candidates lines 1761-1797 — already branches on `domain in {"identity","financial"}` at line 1773; _ONE_EMAIL_ATTR_SCOPE_RE line 62)
  </read_first>
  <action>
    Read `_effective_required_fields_for_candidates` and confirm the `domain == "identity"`
    branch (line 1773) already consumes `extracted_fields` for identity candidates.
    Verify `_IDENTITY_REQUIRED_FIELDS` (lines 100-114) contains `full_name`, `email`,
    `phone_number`, `date_of_birth`, `address` and NO `ssn`. If the identity producer's
    canonical field names (from plan 02-01: `full_name`, `email`, `phone_number`,
    `date_of_birth`, `address`) do not already align with these required-field strings,
    add a small, explicit normalization mapping so producer field keys resolve to the
    KYC required-field names (e.g. ensure `date_of_birth` not `dob` reaches KYC). Do
    NOT add any SSN field or weaken the restricted-tier block. Make the smallest change
    that guarantees an identity payload produced by the Wave 1 schema satisfies the
    KYC required fields; if alignment is already correct, make no code change and
    record that in the summary.
  </action>
  <acceptance_criteria>
    - `one_email_kyc_service.py` `_IDENTITY_REQUIRED_FIELDS` contains `full_name`, `email`, `phone_number`, `date_of_birth`, `address`
    - `one_email_kyc_service.py` `_IDENTITY_REQUIRED_FIELDS` contains NO `ssn` token (`grep -i ssn` of the frozenset block returns nothing)
    - `_effective_required_fields_for_candidates` still branches on `domain in {"identity", "financial"}`
    - The identity domain key used is `identity`, not `kyc_workflow`
  </acceptance_criteria>
  <verify>
    <automated>cd consent-protocol && python3 -c "from hushh_mcp.services.one_email_kyc_service import _IDENTITY_REQUIRED_FIELDS as F; assert {'full_name','email','phone_number','date_of_birth','address'} <= set(F); assert not any('ssn' in f for f in F)"</automated>
  </verify>
  <done>Identity producer field shape provably satisfies KYC required fields; no SSN; no kyc_workflow collision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests — identity fields satisfy KYC required fields and attr.identity.* resolves</name>
  <files>consent-protocol/tests/services/test_one_email_kyc_service.py</files>
  <read_first>
    - consent-protocol/hushh_mcp/services/one_email_kyc_service.py (_effective_required_fields_for_candidates lines 1761-1797; _IDENTITY_REQUIRED_FIELDS lines 100-114; _DEFAULT_KYC_SCOPE line 56; _ONE_EMAIL_ATTR_SCOPE_RE line 62; _ALLOWED_KYC_DATA_SCOPES line 63)
    - consent-protocol/tests/services/test_one_email_kyc_service.py (existing test setup/fixtures for the KYC service — mirror its construction pattern)
  </read_first>
  <behavior>
    - Given an identity candidate (`domain="identity"`, `scope="attr.identity.*"`) and
      extracted_fields `["full_name", "email", "phone_number", "date_of_birth", "address"]`,
      `_effective_required_fields_for_candidates` returns those fields (identity branch
      passes them through).
    - `_DEFAULT_KYC_SCOPE == "attr.identity.*"` and matches `_ONE_EMAIL_ATTR_SCOPE_RE`.
    - `"attr.identity.*"` is in `_ALLOWED_KYC_DATA_SCOPES`.
    - `_IDENTITY_REQUIRED_FIELDS` contains no field whose name includes `ssn`.
  </behavior>
  <action>
    Add tests to `test_one_email_kyc_service.py`:
    (1) `test_identity_candidate_yields_kyc_required_fields`: build an identity
    candidate and call `_effective_required_fields_for_candidates`; assert the returned
    list includes `full_name`, `email`, `phone_number`, `date_of_birth`, `address`.
    (2) `test_attr_identity_scope_resolves`: assert `_DEFAULT_KYC_SCOPE == "attr.identity.*"`,
    that it matches `_ONE_EMAIL_ATTR_SCOPE_RE`, and that it is in `_ALLOWED_KYC_DATA_SCOPES`.
    (3) `test_kyc_required_fields_have_no_ssn`: assert no field in
    `_IDENTITY_REQUIRED_FIELDS` contains the substring `ssn`.
    (4) `test_identity_domain_distinct_from_kyc_workflow`: assert the identity domain
    key used is `"identity"` and is not `"kyc_workflow"`.
    Mirror the existing test construction/fixtures in the file.
  </action>
  <acceptance_criteria>
    - `cd consent-protocol && python3 -m pytest tests/services/test_one_email_kyc_service.py -k "identity or kyc_required or attr_identity" -q` exits 0
    - The required-fields test asserts presence of `full_name`, `email`, `phone_number`, `date_of_birth`, `address`
    - A test asserts no `ssn` substring in `_IDENTITY_REQUIRED_FIELDS`
    - `cd consent-protocol && python3 -m pytest tests/services/test_one_email_kyc_service.py -q` exits 0 (no regression)
  </acceptance_criteria>
  <verify>
    <automated>cd consent-protocol && python3 -m pytest tests/services/test_one_email_kyc_service.py -q</automated>
  </verify>
  <done>Producer→consumer contract is locked by tests: identity fields satisfy KYC, attr.identity.* resolves, no SSN, no kyc_workflow collision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| identity producer payload → KYC consumer | Field-name contract must hold for consent-export to work |
| consent scope `attr.identity.*` → KYC agent | Scope must resolve only to permitted identity data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02C-01 | Repudiation/Contract drift | producer field names vs `_IDENTITY_REQUIRED_FIELDS` | mitigate | Lock the mapping with tests (Task 2); `full_name` canonical per D-B |
| T-02C-02 | Information Disclosure | restricted tier leaking to default_available | accept | Existing force-block verified, not weakened (Task 1 constraint) |
| T-02C-03 | Elevation | identity colliding with internal kyc_workflow domain | mitigate | Distinct domain key asserted by test (Task 2) |
| T-02C-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
- `cd consent-protocol && python3 -m pytest tests/services/test_one_email_kyc_service.py -q` exits 0
- `cd consent-protocol && python3 -c "from hushh_mcp.services.one_email_kyc_service import _IDENTITY_REQUIRED_FIELDS, _DEFAULT_KYC_SCOPE; assert _DEFAULT_KYC_SCOPE=='attr.identity.*'; assert not any('ssn' in f for f in _IDENTITY_REQUIRED_FIELDS)"`
</verification>

<success_criteria>
- Identity producer fields satisfy KYC required fields with zero KYC schema changes
- attr.identity.* scope resolves and is allowed for consent-export
- No SSN field; no kyc_workflow collision
</success_criteria>

<output>
Create `.planning/phases/02-identity-domain/02-03-SUMMARY.md` when done
</output>
