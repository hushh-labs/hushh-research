---
phase: 03-kyc-redraft-intelligence
plan: 04
type: execute
wave: 4
depends_on: ["03-02", "03-03"]
autonomous: false
requirements: [D-A, D-E, D-F, D-H]
files_modified:
  - hushh-webapp/app/one/kyc/page.tsx
  - hushh-webapp/lib/services/__tests__/one-kyc-client-zk-service.redact.test.ts
  - consent-protocol/tests/services/test_one_email_kyc_service_llm.py

must_haves:
  truths:
    - "The Redraft SettingsGroup shows a disclosure banner ('AI will rewrite your draft. Only redacted placeholders are sent — never your actual details.') before the first LLM redraft; the Redraft button is disabled until the user acknowledges"
    - "After acknowledgement the LLM path proceeds; keyword-only instructions bypass the disclosure gate and go directly to the regex path"
    - "There is an explicit UI toggle (e.g. 'Use AI' checkbox) so the user can force the LLM path for an ambiguous instruction or force the regex path for any instruction"
    - "The existing regex redraft transforms (compact/formal/bulletList/etc.) still produce the correct output after this plan"
    - "draft_body remains NULL after an LLM redraft confirmed by a backend test asserting the _update_workflow call args"
    - "A free-form instruction ('rephrase the intro to sound warmer') goes to the LLM path and produces a correctly re-filled draft with all consented fields intact (e2e test or integration scenario)"
    - "A keyword instruction ('make it short') stays on the regex path (no LLM call) verified by the unit test"
    - "_redraft_requests_more_data still blocks scope-expansion instructions after an LLM redraft attempt"
  artifacts:
    - path: "hushh-webapp/app/one/kyc/page.tsx"
      provides: "llmDisclosureAcknowledged state + disclosure banner + useAiRedraft toggle in Redraft SettingsGroup"
      contains: "llmDisclosureAcknowledged"
    - path: "hushh-webapp/lib/services/__tests__/one-kyc-client-zk-service.redact.test.ts"
      provides: "Full unit test suite for redact/resubstitute/integrity helpers + routing"
      contains: "validateTokenIntegrity"
    - path: "consent-protocol/tests/services/test_one_email_kyc_service_llm.py"
      provides: "Backend unit tests: consent gate, no-persist, scope-expansion block, token-preserving prompt"
      contains: "test_redraft_llm_rejects_missing_scope"
  key_links:
    - from: "llmDisclosureAcknowledged state"
      to: "LLM branch in runAction('redraft')"
      via: "!llmDisclosureAcknowledged gates LLM path entry; shows disclosure banner instead"
      pattern: "llmDisclosureAcknowledged"
    - from: "useAiRedraft toggle"
      to: "isKeywordOnlyInstruction routing"
      via: "useAiRedraft=true forces LLM path regardless of keyword detection; useAiRedraft=false forces regex path"
      pattern: "useAiRedraft"
---

<objective>
Complete the KYC Redraft Intelligence feature by adding the consent/disclosure UI, an explicit
routing override toggle, and the full test suite. This wave wires the remaining D-E (consent
disclosure before first LLM use) and D-F (explicit override) requirements, then validates the
entire pipeline with backend and frontend unit tests plus e2e scenarios.

Purpose: The pipeline is functional after Waves 2–3 but lacks the mandatory user disclosure
(D-E) that must precede the first LLM redraft and the routing override toggle (D-F). Tests
verify every critical invariant: no-persist, scope gate, token integrity, field re-validation,
routing, and regression of the existing regex path.

Output: Two new UI state variables in `page.tsx` (disclosure acknowledgement + AI toggle);
a disclosure banner in the Redraft SettingsGroup; a complete frontend unit test file; a
complete backend unit test file. Human checkpoint verifies the end-to-end visual flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-kyc-redraft-intelligence/03-CONTEXT.md
@.planning/phases/03-kyc-redraft-intelligence/03-RESEARCH.md
@.planning/phases/03-kyc-redraft-intelligence/03-02-PLAN.md
@.planning/phases/03-kyc-redraft-intelligence/03-03-PLAN.md
</context>

<constraints>
- D-E disclosure text MUST be exactly: "AI will rewrite your draft. Only redacted
  placeholders are sent — never your actual details." — word-for-word per the decision.
- D-E gate: the Redraft button must remain disabled for LLM-path instructions until the
  user clicks the disclosure acknowledgement. Keyword-only instructions bypass the
  disclosure entirely (they stay local and never involve the LLM).
- D-F override: the `useAiRedraft` toggle must allow (a) forcing LLM path for an otherwise
  keyword-detected instruction, and (b) forcing regex path for an otherwise free-form
  instruction. The toggle state must participate in the `isKeywordOnlyInstruction` routing
  decision inside `runAction("redraft")`.
- D-H human review mandatory: the rewritten draft is set as `localDraft` for human review;
  the send flow is unchanged (`send-approved-reply`). No auto-send test or path may exist.
- Tests must NOT mock the zero-knowledge guarantee away — backend tests use a mock Gemini
  client that returns a template with correct tokens; they assert `draft_body` is never in
  the `_update_workflow` call args.
- No new npm or pip packages. No DB migrations.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Add disclosure banner + useAiRedraft override toggle to Redraft SettingsGroup</name>
  <files>hushh-webapp/app/one/kyc/page.tsx</files>
  <read_first>
    - hushh-webapp/app/one/kyc/page.tsx lines 310-330 (existing useState declarations — add new state here)
    - hushh-webapp/app/one/kyc/page.tsx lines 954-1031 (runAction 'redraft' arm — routing branch added in Wave 3; the new state variables plug in here)
    - hushh-webapp/app/one/kyc/page.tsx lines 1952-1985 (Redraft SettingsGroup — add disclosure banner and toggle inside this block)
    - hushh-webapp/app/one/kyc/page.tsx lines 395-405 (selectedCanReviewDraft gate — disclosure banner should only render inside this gate, as the Redraft group already does)
  </read_first>
  <action>
    In the state declarations block (near line 317):
      const [llmDisclosureAcknowledged, setLlmDisclosureAcknowledged] = useState(false);
      const [useAiRedraft, setUseAiRedraft] = useState<boolean | null>(null);
      // null = auto-detect; true = force LLM; false = force regex

    In `runAction("redraft")`, update the routing condition to incorporate the override.
    Replace the Wave-3-added line:
      `const isKeyword = isKeywordOnlyInstruction(redraftInstructions.trim());`
    with:
      `const isKeyword = useAiRedraft === false
        ? true  // force regex
        : useAiRedraft === true
          ? false  // force LLM
          : isKeywordOnlyInstruction(redraftInstructions.trim());`

    Before entering the LLM branch (the `if (!isKeyword && localDraft)` block from Wave 3),
    add a disclosure gate:
      if (!llmDisclosureAcknowledged) {
        // Show the disclosure banner (handled in UI) and do not proceed
        setError(null); // clear any prior error
        setBusy(null);
        return;
      }

    In the Redraft SettingsGroup JSX (lines 1952-1985), inside the outer `<div className="space-y-3 ...">`:

    1. Before the `<Textarea>`, add the AI override toggle:
       A `<label>` containing a checkbox (HTML `<input type="checkbox">` or the existing
       Checkbox component if present in the design system) with three states collapsed into
       a `<select>` or a pair of icon buttons. Use the simplest available component:
         - A `<select>` with options: `value=""` ("Auto-detect"), `value="ai"` ("Use AI"),
           `value="regex"` ("Use keywords only").
         - On change: `setUseAiRedraft(val === "ai" ? true : val === "regex" ? false : null)`.
         - Default selected: `""` (auto-detect).
       Label text: "Rewrite mode"

    2. Conditionally render a disclosure banner between the toggle and the `<Textarea>` when:
       `!isKeywordOnlyInstruction(redraftInstructions.trim()) && !llmDisclosureAcknowledged && (useAiRedraft !== false)`
       (i.e. instruction is or will be LLM-routed AND disclosure not yet acknowledged AND not forced to regex).
       Banner content:
         - Text (exactly): "AI will rewrite your draft. Only redacted placeholders are sent — never your actual details."
         - A button labeled "I understand" or "Acknowledge" that calls `setLlmDisclosureAcknowledged(true)`.
       Use the existing `<SettingsRow>` or a simple `<div>` with `role="alert"` consistent
       with the existing alert patterns in the file (e.g. the AlertTriangle block at lines 1944-1949).

    3. Update the existing Redraft `<Button>` `disabled` condition to add:
       `|| (!isKeywordOnlyInstruction(redraftInstructions.trim()) && !llmDisclosureAcknowledged && useAiRedraft !== false)`
       so the button remains disabled for LLM-path instructions until the disclosure is acknowledged.

    Reset `llmDisclosureAcknowledged` to false and `useAiRedraft` to null when
    `setRedraftInstructions("")` is called after a successful redraft (so each new session
    starts fresh). Add these resets alongside `setRedraftInstructions("")` in the runAction
    handler (both the LLM early-return path and the existing keyword path at line 1029).
  </action>
  <acceptance_criteria>
    - `grep -n "llmDisclosureAcknowledged\|useAiRedraft" hushh-webapp/app/one/kyc/page.tsx | wc -l` returns >= 8 occurrences (state declaration + routing + disclosure gate + JSX render + disable condition + resets)
    - `grep -n "Only redacted placeholders are sent" hushh-webapp/app/one/kyc/page.tsx` returns exactly one line (the disclosure text)
    - `grep -n "never your actual details" hushh-webapp/app/one/kyc/page.tsx` returns exactly one line
    - `grep -n "Rewrite mode" hushh-webapp/app/one/kyc/page.tsx` returns the toggle label
    - `cd hushh-webapp && npm run typecheck` exits 0
    - The Redraft Button disabled condition still references `!redraftInstructions.trim()` and `!selectedDraft` (existing guards unchanged)
  </acceptance_criteria>
  <verify>
    <automated>cd hushh-webapp && npm run typecheck</automated>
  </verify>
  <done>Disclosure banner with exact D-E text renders before first LLM redraft; Redraft button disabled until acknowledged; AI/keyword/auto toggle wired to routing; resets on success.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete frontend unit tests and backend unit tests</name>
  <files>hushh-webapp/lib/services/__tests__/one-kyc-client-zk-service.redact.test.ts, consent-protocol/tests/services/test_one_email_kyc_service_llm.py</files>
  <read_first>
    - hushh-webapp/lib/services/__tests__/one-kyc-client-zk-service.redact.test.ts (file created in Wave 3 Task 1 — extend, do not replace; review existing test cases before adding)
    - consent-protocol/tests/services/test_pkm_agent_lab_service.py lines 1-50 (AsyncMock + monkeypatch pattern for the backend service tests — mirror this structure)
    - consent-protocol/hushh_mcp/services/one_email_kyc_service.py (redraft_llm method from Wave 2; validate_token_with_db call; _redraft_requests_more_data guard; _update_workflow kwargs — no draft_body)
    - consent-protocol/tests/services/test_one_email_kyc_service_llm.py (file created in Wave 2 — extend if it exists, create if not; check line count before deciding)
  </read_first>
  <behavior>
    Frontend unit tests (extend the .test.ts file):
    - Routing: isKeywordOnlyInstruction("make it short") === true; isKeywordOnlyInstruction("rephrase the intro to sound warmer") === false; isKeywordOnlyInstruction("shorter and warmer") === false; isKeywordOnlyInstruction("bullet list and more formal") === true (both are pure keywords).
    - Round-trip: redactDraftForLlm then resubstituteDraft recovers the exact original body string.
    - Completeness assertion: redactDraftForLlm throws when a value >= 3 chars remains in tokenizedTemplate (simulate by passing a body that does NOT contain the value — the assertion still fires because values are replaced via replaceAll; test the throw by passing a body that contains a value that was NOT replaced due to a hypothetical bug — use a manually crafted scenario or spy on replaceAll).
    - Integrity: validateTokenIntegrity returns false for dropped token, duplicated token, invented token; returns true for clean round-trip.
    - useAiRedraft override: when useAiRedraft=true, a keyword instruction ("make it short") enters the LLM branch (test the routing logic as a pure function by calling the routing expression directly, not via the React component).

    Backend unit tests (test_one_email_kyc_service_llm.py):
    - test_redraft_llm_rejects_missing_scope: patch validate_token_with_db to return (False, "no scope", None); call redraft_llm; assert PermissionError raised.
    - test_redraft_llm_rejects_wrong_workflow_state: patch get_workflow to return status="drafting"; assert OneEmailKycError raised with code ONE_KYC_DRAFT_NOT_READY.
    - test_redraft_llm_blocks_scope_expansion: patch get_workflow to return valid state; set instruction="also include my portfolio"; assert OneEmailKycError with code ONE_KYC_LLM_SCOPE_EXPANSION_BLOCKED.
    - test_redraft_llm_no_draft_body_in_update: patch validate_token_with_db (valid), _require_gemini_ready (True), _gemini_client.models.generate_content to return a mock response with .text="Hello {{F0}}"; call redraft_llm with tokenized_template="Hello {{F0}}", instruction="shorter"; capture the _update_workflow call kwargs; assert "draft_body" NOT in kwargs.
    - test_redraft_llm_metadata_updated: same mock setup; assert returned metadata has "last_redraft_instruction_hash" (a 64-char hex string) and "draft_revision" incremented.
    - test_redraft_llm_token_preserving_prompt: same mock setup; capture the system_instruction passed to generate_content; assert it contains the substring "Preserve EVERY placeholder token exactly".
    - test_redraft_llm_returns_rewritten_template: same mock; assert return value is {"rewritten_template": "Hello {{F0}}"}.

    Regression tests (add to existing test files or new file):
    - Backend: test that calling the existing redraft() (not redraft_llm()) with instruction "make it formal" still updates draft_revision and last_redraft_instruction_hash and does NOT call generate_content.
    - Frontend: isKeywordOnlyInstruction still classifies all 8 existing keyword-vocabulary tokens (compact, formal, bulletList, structured, table, fullDetail, human, cleanHeaders) as keyword-only (use the test instruction strings that map to each).
  </behavior>
  <action>
    Extend `hushh-webapp/lib/services/__tests__/one-kyc-client-zk-service.redact.test.ts`:
    Add the routing override test and the completeness assertion throw test as new `it()` blocks.
    Add regression tests for all 8 keyword classes to confirm isKeywordOnlyInstruction handles them.
    Import `isKeywordOnlyInstruction` alongside the other helpers (already imported in Wave 3).

    Create or extend `consent-protocol/tests/services/test_one_email_kyc_service_llm.py`:
    Follow the monkeypatch + AsyncMock pattern from `test_pkm_agent_lab_service.py`. Use
    `unittest.mock.patch` or pytest monkeypatch to patch:
      - `hushh_mcp.services.one_email_kyc_service.validate_token_with_db`
      - `hushh_mcp.services.one_email_kyc_service._require_gemini_ready`
      - `hushh_mcp.services.one_email_kyc_service._gemini_client`
    For the Gemini mock: create a `MagicMock()` with `.models.generate_content.return_value`
    set to a mock object with `.text = "Hello {{F0}}"`. Capture `_update_workflow` calls with
    `AsyncMock` or `MagicMock` (depending on whether `_update_workflow` is async — read the
    service to confirm).
    Each test function is standalone `async def test_...` decorated with `@pytest.mark.asyncio`.
  </action>
  <acceptance_criteria>
    - `cd hushh-webapp && npx jest one-kyc-client-zk-service.redact --no-coverage` exits 0 with all new tests passing
    - `grep -c "it(" hushh-webapp/lib/services/__tests__/one-kyc-client-zk-service.redact.test.ts` returns >= 12 (original Wave 3 tests + new Wave 4 tests)
    - `cd consent-protocol && python3 -m pytest tests/services/test_one_email_kyc_service_llm.py -q` exits 0 with all 7+ tests passing
    - `grep -n "test_redraft_llm_rejects_missing_scope\|test_redraft_llm_no_draft_body" consent-protocol/tests/services/test_one_email_kyc_service_llm.py` returns both test function names
    - `grep -n "draft_body" consent-protocol/tests/services/test_one_email_kyc_service_llm.py | grep "assert\|NOT\|not in"` returns the no-persist assertion line
    - `grep -n "Preserve EVERY placeholder" consent-protocol/tests/services/test_one_email_kyc_service_llm.py` returns the prompt assertion line
    - Regression: `cd consent-protocol && python3 -m pytest tests/services/test_one_email_kyc_service_llm.py -k "regression" -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd consent-protocol && python3 -m pytest tests/services/test_one_email_kyc_service_llm.py -q</automated>
  </verify>
  <done>Full test suite passes: 7 backend tests (scope gate, state check, scope expansion block, no-persist, metadata update, token-preserving prompt, return shape) + 12+ frontend unit tests (routing, round-trip, integrity, keyword regression).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of end-to-end Redraft Intelligence UI flow</name>
  <what-built>
    All four waves are complete:
    - Wave 1: agent.kyc.redraft.llm scope + manifest carve-out flag
    - Wave 2: POST /api/one/kyc/workflows/{id}/redraft-llm backend proxy (consent-gated, no-persist)
    - Wave 3: client-side redact → rewrite → re-fill pipeline with integrity and field re-validation
    - Wave 4: disclosure banner + AI/keyword/auto routing toggle + full test suite
  </what-built>
  <how-to-verify>
    Start the backend (consent-protocol) and frontend (hushh-webapp) in development mode.
    Navigate to One > KYC > an active workflow in `waiting_on_user` / `draft_status=ready` state.

    1. KEYWORD PATH (no LLM):
       - Type "make it shorter" in the Redraft box.
       - Confirm the disclosure banner does NOT appear (keyword = local path).
       - Click "Redraft" and confirm the draft updates without any network call to /redraft-llm
         (check DevTools Network tab — no call to that endpoint).
       - Confirm the draft is shorter / more compact.

    2. DISCLOSURE GATE:
       - Type "rephrase the intro to sound warmer" in the Redraft box.
       - Confirm the disclosure banner appears with the exact text:
         "AI will rewrite your draft. Only redacted placeholders are sent — never your actual details."
       - Confirm the "Redraft" button is disabled.
       - Click "I understand" / "Acknowledge".
       - Confirm the banner disappears and the "Redraft" button becomes enabled.

    3. LLM PATH (after acknowledgement):
       - With the same instruction "rephrase the intro to sound warmer", click "Redraft".
       - Confirm a call to POST /api/one/kyc/workflows/{id}/redraft-llm appears in DevTools
         Network. The request body should contain "tokenized_template" with no real name/address
         values (only {{F0}}, {{F1}}, etc. placeholders).
       - Confirm the draft preview updates with a rewritten version.
       - Confirm all consented fields are still present in the draft (no field dropped).

    4. AI OVERRIDE TOGGLE:
       - Set the "Rewrite mode" selector to "Use AI".
       - Type "make it shorter" (a keyword instruction).
       - Confirm the disclosure banner appears (because the toggle forces LLM path).
       - After acknowledging, confirm /redraft-llm is called.
       - Set the selector back to "Use keywords only".
       - Type "rephrase the intro to sound warmer".
       - Confirm the disclosure banner does NOT appear and /redraft-llm is NOT called (regex path).

    5. SEND FLOW UNCHANGED:
       - After a LLM redraft, confirm the "Approve and send" button is present and functional.
       - Confirm no auto-send occurs — the draft requires manual approval.

    6. REGRESSION:
       - Confirm existing keyword redrafts (formal, bullet list, compact) still work correctly.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 verification steps pass. If any step fails, describe which step and the observed behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user instruction → routing logic | Determines whether LLM or regex path is taken; disclosure gate enforces informed consent |
| LLM rewritten draft → user review | Untrusted output; shown for human review before send; auto-send is not possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Repudiation | User claims LLM used without informed consent | mitigate | Disclosure banner with exact D-E text must be acknowledged before any LLM call; llmDisclosureAcknowledged state gates the branch (Task 1) |
| T-03-04-02 | Tampering | Auto-send of LLM-rewritten draft | mitigate | send-approved-reply is the only send path; no auto-send logic added; checkpoint verifies (Task 3 step 5) |
| T-03-04-03 | Tampering | Regression: existing regex transforms broken | mitigate | Keyword regression tests cover all 8 transform classes; checkpoint step 6 verifies visual output (Task 2 + Task 3) |
| T-03-04-04 | Information Disclosure | Test suite mocks away ZK guarantee | mitigate | Backend tests mock Gemini only (not the consent gate or the no-persist guard); draft_body absence is asserted in mock call args (Task 2) |
| T-03-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; tests use existing jest and pytest infra |
</threat_model>

<verification>
- `cd hushh-webapp && npm run typecheck` exits 0
- `cd hushh-webapp && npx jest one-kyc-client-zk-service.redact --no-coverage` exits 0
- `cd consent-protocol && python3 -m pytest tests/services/test_one_email_kyc_service_llm.py -q` exits 0
- `grep -n "Only redacted placeholders are sent" hushh-webapp/app/one/kyc/page.tsx` returns exactly one line with the exact D-E disclosure text
- `grep -n "llmDisclosureAcknowledged" hushh-webapp/app/one/kyc/page.tsx | wc -l` returns >= 5
- Human checkpoint (Task 3): all 6 steps approved
- Regression: `grep -n "redraftTransformFromInstructions" hushh-webapp/lib/services/one-kyc-approved-disclosure-renderer.ts` returns same line count as before any wave (function unmodified)
</verification>

<success_criteria>
- Disclosure banner with exact D-E text shown before first LLM redraft; Redraft button disabled until acknowledged
- AI/keyword/auto toggle overrides routing per D-F
- Full backend test suite: scope gate, no-persist, scope-expansion block, prompt integrity, return shape — all passing
- Full frontend unit suite: routing, round-trip, integrity, keyword regression — all passing
- Existing regex redraft path regression-tested and visually verified
- Human checkpoint approved (end-to-end visual flow, send-flow unchanged, no auto-send)
</success_criteria>

<output>
Create `.planning/phases/03-kyc-redraft-intelligence/03-04-SUMMARY.md` when done
</output>
