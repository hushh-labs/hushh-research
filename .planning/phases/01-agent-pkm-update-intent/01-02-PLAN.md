---
phase: "01-agent-pkm-update-intent"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - hushh-webapp/components/agent/agent-pkm-review-panel.tsx
autonomous: true
requirements:
  - D-08
  - D-09

must_haves:
  truths:
    - "AgentPkmReviewPanel accepts optional `mode` and `updateContext` props with backward-compatible defaults"
    - "When mode is `update`, the header reads 'Update PKM?' and the description text describes a proposed change"
    - "When mode is `update` and updateContext is provided, a diff block shows Current value and Proposed value side by side"
    - "When mode is `update`, the confirm button label reads 'Update' instead of 'Save'"
    - "When mode is `add` (default), the panel renders exactly as before — no visual change"
    - "All existing AgentPkmReviewPanel call sites compile without modification"
  artifacts:
    - path: "hushh-webapp/components/agent/agent-pkm-review-panel.tsx"
      provides: "Extended AgentPkmReviewPanelProps with mode and updateContext; update-mode diff block"
      exports: ["AgentPkmReviewPanel"]
    - path: "hushh-webapp/components/agent/__tests__/agent-pkm-review-panel.test.tsx"
      provides: "Render tests for add mode (regression) and update mode (new)"
  key_links:
    - from: "agent-pkm-review-panel.tsx props"
      to: "agent-chat-workspace.tsx render site"
      via: "mode and updateContext props passed from pkmReviews state (Wave 3)"
      pattern: "mode={review\\.updateContext.*\"update\".*\"add\"}"
---

<objective>
Extend `agent-pkm-review-panel.tsx` with two new optional props: `mode?: "add" | "update"` (default `"add"`) and `updateContext?: { domain, fieldPath, currentValue, proposedValue }`. When `mode === "update"`, the panel shows an alternate header, description, a current → proposed diff block, and relabels the confirm button to "Update".

Purpose: Per D-08 and D-09, the existing panel is the only review surface — no new panel component. Backward compatibility is mandatory: all existing `<AgentPkmReviewPanel>` call sites must continue to compile and render identically with no prop changes.

Output: Modified `agent-pkm-review-panel.tsx` with update mode support and a test file confirming both add-mode regression and update-mode rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@hushh-webapp/components/agent/agent-pkm-review-panel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend `AgentPkmReviewPanelProps` type and implement update-mode render branches</name>
  <files>
    hushh-webapp/components/agent/agent-pkm-review-panel.tsx
    hushh-webapp/components/agent/__tests__/agent-pkm-review-panel.test.tsx
  </files>
  <read_first>
    - hushh-webapp/components/agent/agent-pkm-review-panel.tsx — read in full (all ~129 lines). Identify: the current props type block (lines ~9–15), the destructuring line, the `if (cards.length === 0) return null` guard, the header `<p>` elements, the card iteration block, and the button JSX. Also note the `cleanText` and `titleize` utility functions already in this file — both must be used in the diff block.
  </read_first>
  <behavior>
    - Render with mode="add" (default) + cards → header "Save to PKM?", description contains "needs your review before it is stored", button label "Save"
    - Render with mode="update" + updateContext → header "Update PKM?", description contains "proposes a change to your saved record"
    - Render with mode="update" + updateContext → diff block present: text "Current" and "Proposed" both visible
    - Render with mode="update" + updateContext where currentValue="Alice" proposedValue="Bob" → "Alice" and "Bob" both appear in the rendered output
    - Render with mode="update" + updateContext → confirm button label is "Update"
    - Render with no props for mode/updateContext → behaves identically to the pre-change panel (regression: header "Save to PKM?", button label "Save")
    - Render with mode="update" and cards.length === 0 → does NOT return null (the cards guard is relaxed for update mode)
  </behavior>
  <action>
    Write the test file first at `hushh-webapp/components/agent/__tests__/agent-pkm-review-panel.test.tsx` covering all seven behavior cases above using React Testing Library (`render`, `screen.getByText`, `screen.queryByText`). Mock `onSave` and `onDismiss` as `jest.fn()`. Use a minimal stub `AgentPkmPreviewCard` with `write_mode: "confirm_first"` for add-mode tests.

    Then modify the panel file:

    1. Extend the props type (after the existing closing brace of `AgentPkmReviewPanelProps`):
       Add `mode?: "add" | "update"` and `updateContext?: { domain: string; fieldPath: string; currentValue: string; proposedValue: string }` as optional fields. Do NOT remove or rename any existing fields.

    2. Add `mode = "add"` and `updateContext` to the destructuring parameters with the `mode` default.

    3. Update the null guard: change `if (cards.length === 0) return null` to `if (cards.length === 0 && mode !== "update") return null` so update-mode panels (which may have zero preview cards) still render.

    4. In the header section, branch `mode === "update"` for both the title `<p>` ("Update PKM?" vs "Save to PKM?") and the description `<p>` ("Agent proposes a change to your saved record. Review before applying." vs existing text). Keep all existing className values unchanged.

    5. After the header section and before the cards list, insert the diff block: conditionally rendered when `mode === "update" && updateContext`. The block uses a `div` with `className="mt-3 rounded-md border border-border/60 bg-background p-2 text-xs"`. Inside: a `<p>` with `titleize(updateContext.fieldPath.replace(/\./g, " "))` as the label; a flex row with two columns and an arrow `→` between them; the left column shows `<p className="text-muted-foreground">Current</p>` and `<p className="mt-0.5 text-foreground">{cleanText(updateContext.currentValue)}</p>`; the right column shows `<p className="text-muted-foreground">Proposed</p>` and `<p className="mt-0.5 font-medium text-foreground">{cleanText(updateContext.proposedValue)}</p>`. A final `<p className="mt-1 text-muted-foreground">Domain: {titleize(updateContext.domain)}</p>`.

    6. In the confirm button, change the static label string "Save" to `{mode === "update" ? "Update" : "Save"}`.

    Do NOT modify any other JSX, classNames, or logic in the file. The add-mode render path must remain byte-for-byte equivalent.
  </action>
  <verify>
    <automated>cd hushh-webapp && npx jest --testPathPattern="agent-pkm-review-panel" --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All seven behavior tests pass
    - `grep -c 'mode.*"update"' hushh-webapp/components/agent/agent-pkm-review-panel.tsx` returns at least 3 (header, diff block, button)
    - `grep -c '"Save to PKM?"' hushh-webapp/components/agent/agent-pkm-review-panel.tsx` returns 1 (not removed, now inside ternary)
    - `grep -c '"Update PKM?"' hushh-webapp/components/agent/agent-pkm-review-panel.tsx` returns 1
    - `grep -n 'mode.*=.*"add"' hushh-webapp/components/agent/agent-pkm-review-panel.tsx` shows default assignment in destructuring
    - `cd hushh-webapp && npx tsc --noEmit 2>&1 | grep "agent-pkm-review-panel"` returns no errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM slots → panel display | `currentValue` and `proposedValue` are LLM-generated strings displayed directly in the review panel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Diff block display | mitigate | Use `cleanText` (already in file) to sanitize `currentValue` and `proposedValue` before rendering — prevents raw control characters or excessive whitespace from escaping the UI frame |
| T-02-02 | Tampering | `updateContext.fieldPath` display | accept | `fieldPath` is passed from frontend slot handler (Wave 3) which validates it as a string; the panel only renders it via `titleize()` — no eval or code execution |
| T-02-03 | Spoofing | Panel mode selection | accept | `mode` prop is set by the workspace controller based on `review.updateContext` presence — user cannot override it; only valid values are `"add"` and `"update"` per TypeScript union type |
| T-02-SC | Tampering | npm/pip installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. `cd hushh-webapp && npx jest --testPathPattern="agent-pkm-review-panel" --passWithNoTests` — all tests pass
2. `grep -n "mode.*\"update\"" hushh-webapp/components/agent/agent-pkm-review-panel.tsx` — at least 3 occurrences
3. `grep -n "updateContext" hushh-webapp/components/agent/agent-pkm-review-panel.tsx` — appears in type, destructuring, and diff block
4. `grep -n "mode = \"add\"" hushh-webapp/components/agent/agent-pkm-review-panel.tsx` — default assignment present
5. `cd hushh-webapp && npx tsc --noEmit 2>&1 | grep "agent-pkm-review-panel"` — no TypeScript errors
6. Visual regression: existing `<AgentPkmReviewPanel cards={[card]} onSave={fn} onDismiss={fn} />` call (no mode prop) renders header "Save to PKM?" and button "Save" in test output
</verification>

<success_criteria>
- `AgentPkmReviewPanelProps` has `mode?: "add" | "update"` and `updateContext?` fields
- Default (`mode` omitted) renders identically to pre-change behavior — regression tests pass
- Update mode renders diff block with "Current" and "Proposed" labels and the respective values
- Update mode renders "Update PKM?" header and "Update" button label
- TypeScript emits no errors for this file
</success_criteria>

<output>
Create `.planning/phases/01-agent-pkm-update-intent/01-02-SUMMARY.md` when done
</output>
