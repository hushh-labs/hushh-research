import { CalendarAgentPage } from "@/components/calendar/calendar-agent-page";

/** Calendar is a first-class One agent, not a profile integration. */
export default function CalendarPage() {
  return <CalendarAgentPage />;
}
