"""Referral program domain rules.

Pure functions only. Nothing here reads a database, a clock or a request, so
every rule below can be tested exhaustively -- including with generated input.
The service layer owns all I/O.
"""
