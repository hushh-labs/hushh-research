# 🔐 Crypto + Signing
# SECURITY: Updated from 42.0.5 to fix CVE-2024-26130 (vulnerable OpenSSL in wheels)
cryptography>=44.0.0
pyjwt[crypto]==2.10.1

# 🧪 Validation (use v2 binary wheel)
pydantic>=2.0,<3.0

# 🧬 Environment management
python-dotenv==1.2.1

# 🚀 Web Server
fastapi>=0.115.0
uvicorn>=0.34.0

# 🗃️ Database (Postgres async)
asyncpg>=0.29.0

# ⚡ HTTP client for agents (e.g. Apple ID, APIs)
httpx==0.28.1

# 🛠️ CLI + scripting
argparse==1.4.0

# 🧪 Testing
pytest==9.0.2

# 🔍 Code Quality & Linting
# 🔍 Code Quality & Linting moved to requirements-dev.txt

# 🚦 Rate Limiting & Real-time
slowapi>=0.1.9           # FastAPI rate limiting
sse-starlette>=2.1.0     # Server-Sent Events for consent notifications

# 📦 Optional OAuth / Apple ID agents
# SECURITY: Updated from 2.32.3 to fix CVE-2024-35195 (.netrc credentials leak)
requests>=2.32.4

# 🔌 MCP Server SDK (Model Context Protocol)
mcp>=0.9.0

# 🔥 Firebase Admin SDK (Identity)
firebase-admin>=6.6.0  # Updated to recent stable

# 🔧 Protocol Buffers & GRPC (Pinning to avoid resolution-too-deep errors)
# These versions are known to work well with google-api-core and firebase-admin
# 🔧 Protocol Buffers & GRPC (Stable Protobuf 4 Set)
# Anchoring to Protobuf 4.x to resolve conflicts between firebase-admin and google-adk
protobuf==6.33.4
grpcio==1.76.0
grpcio-status==1.76.0
google-api-core==2.29.0
proto-plus==1.27.0

# 🧠 Generative AI (ADK - Agent Development Kit)
# Using google-adk which includes google-genai SDK
google-adk>=1.23.0

# 🛠️ Utilities
wrapt>=1.14.0  # Required for Python 3.11+ compatibility
rsa>=4.9
google-auth>=2.14.1

# 📈 Market Data
yfinance>=0.2.36
